import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Foodie Yellow Palette
  static const Color primary = Color(0xFFFFC107); // Warm Foodie Yellow
  static const Color primaryDark = Color(0xFFFFA000);
  static const Color primaryLight = Color(0xFFFFECB3);
  static const Color primarySoft = Color(0xFFFFF8E1);

  // Secondary & Food Accents
  static const Color secondary = Color(0xFFFF6B00); // Food Accent Orange
  static const Color secondaryDark = Color(0xFFE65100);
  static const Color secondaryLight = Color(0xFFFFE0B2);

  // Background & Surfaces
  static const Color background = Color(0xFFFFFDF7); // Soft Warm Cream
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFFAF8F2);
  static const Color cardShadow = Color(0x0D000000);

  // Text Colors
  static const Color textPrimary = Color(0xFF18181B); // Charcoal
  static const Color textSecondary = Color(0xFF71717A);
  static const Color textMuted = Color(0xFFA1A1AA);
  static const Color textLight = Color(0xFFFFFFFF);

  // Status & Utility Colors
  static const Color success = Color(0xFF10B981);
  static const Color successLight = Color(0xFFD1FAE5);
  static const Color warning = Color(0xFFF59E0B);
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color error = Color(0xFFE11D48);
  static const Color errorLight = Color(0xFFFFE4E6);
  static const Color info = Color(0xFF3B82F6);

  // Rating & Favorites
  static const Color starGold = Color(0xFFFFA000);
  static const Color favoriteRed = Color(0xFFE11D48);

  // Border & Divider Colors
  static const Color border = Color(0xFFECE8DC);
  static const Color borderFocused = Color(0xFFFFC107);
  static const Color divider = Color(0xFFF3F0E6);
}
